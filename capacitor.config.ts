import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ms-users-front',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
